export enum UserStatus {
  ENABLE = 'ENABLE',
  DISABLE = 'DISABLE',
}

export enum SerieTeam {
  A = 'A',
  B = 'B',
  C = 'C',
}

export enum Gender {
  MASCULINO = 'M',
  FEMENINO = 'F',
  OTRO = 'O',
}
