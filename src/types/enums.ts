export enum UserStatus {
  ENABLE = 'ENABLE',
  DISABLE = 'DISABLE',
}

export enum SerieTeam {
  A = 'A',
  B = 'B',
  C = 'C',
}

export enum Gender {
  MASCULINO = 'M',
  FEMENINO = 'F',
  OTRO = 'O',
}

export enum Result {
  HOME = 'V',
  AWAY = 'L',
  DRAW = 'E',
}
